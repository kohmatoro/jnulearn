import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import "./App.css";

const root = ReactDOM.createRoot(document.getElementById("root"));

// 괄호 안에 <App />을 넣어야 화면에 그림이 그려집니다.
root.render(
    <App />
);